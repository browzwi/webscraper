spring:
  profiles:
    active: test
  datasource:
    url: jdbc:h2:mem:webscraper;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

webscraper:
  security:
    admin:
      username: admin
      plain-password: admin
